version = 0.1

[default.deploy.parameters]
stack_name = "security-agent"
capabilities = "CAPABILITY_IAM"
resolve_s3 = true
confirm_changeset = true
# Example parameter overrides (edit and uncomment as needed):
# parameter_overrides = "MinSeverity=HIGH Collectors=guardduty,securityhub,compliance SlackWebhookUrl=https://hooks.slack.com/services/XXX NotificationEmail=sec@example.com"
